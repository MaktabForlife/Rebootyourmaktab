# V105.3.1 verification

- Full backend regression: **72/72 test files passed**. Development Worker bundle dry run passed.
- Program management domain checks: optional levels, subject scope, duplicate names/links, dated membership validation, archive guards, stale revisions, concurrent saves, authority revocation, retry/recovery and delayed old writes.
- API/Sheets integration: central GLOBAL_ADMIN required; local admins, teachers/student tokens and anonymous requests denied. Prepared table migration, saved management rows reflected in timetable catalog, cross-subject levels rejected, central account privileges untouched, known membership dates validated, archived teachers blocked, published snapshots and legacy Reboot records retained.
- Actual local Cloudflare runtime: bundled Worker with SQLite Durable Object and synthetic Google transport passes the extended API integration suite. No live spreadsheet writes.
- Browser: compact six-section grid; shared subject linking; optional subject level; module without level; class creation; existing account teacher assignment; dated learner membership; Ctrl/Command+Enter save; simulated failed save retains row and retry succeeds; timetable sees newly saved module and teacher; 390px layout keeps navigation and grids horizontally scrollable.
- Timezones: native dropdowns in Program setup and timetable; Johannesburg default; saved choices survive reload; saved legacy aliases and blank choices preserved; fallback choices for older browsers; output escaped.

Deployment and real-data acceptance are separate from these local checks. No live timetable was published by these tests.
